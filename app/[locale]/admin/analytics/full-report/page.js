import { redirect } from "next/navigation";

export default function AdminAnalyticsFullReportPage() {
  // Some widgets link to a "full report" page; redirect to analytics until implemented.
  redirect("/admin/analytics");
}
