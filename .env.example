# Example environment configuration (copy to .env.local)

# Postgres (primary DB)
# Preferred (most hosting providers give you this)
DATABASE_URL=

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Optional (production defaults to SSL on). Set to false only if your DB does not support SSL.
DB_SSL=true

# JWT
JWT_SECRET=change-me-to-a-long-random-string

# Next.js / Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me

# Optional legacy Mongo (if used by any older code)
MONGO_URL=mongodb://localhost:27017
# DB_NAME=your_mongo_db_name

# Public base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# CORS (if used)
CORS_ORIGINS=*
